import { StyleSheet } from "react-native";

const style = StyleSheet.create({
  textInput: {
    borderWidth: 1,
    borderColor: "#014949",
    borderRadius: 4,
    padding: 15,
    marginBottom: 10,
    marginTop: 10,
  },
});

export default style;
